<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.web.tl.dao.UserDao">

    <select id="loginByaccount" parameterType="string" resultType="cn.web.tl.entity.Account">
        SELECT * FROM `account` WHERE account=#{account}
    </select>

    <select id="loginByphone" parameterType="string"  resultType="cn.web.tl.entity.Account">

         SELECT * FROM `account` WHERE phone=#{phone}
    </select>

    <insert id="addUser">

    </insert>

    <insert id="addByphone">

    </insert>



</mapper>